import styled from 'styled-components';

export default styled.div`
  display: flex;
  align-items: center;
  padding-left: var(--space-375);
  padding-right: var(--space-375);
  flex: 0 0 var(--space-450);
  border-bottom: 1px solid var(--background-secondary);
`;
